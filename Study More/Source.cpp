#include <iostream>
#include "header.h"
#include <random>

using namespace std;



int main()
{
	/*
	int red, kol;
	cin >> red >> kol;
	double** niz = new double*[red];

	for (int i = 0; i < red; i++)
		niz[i] = new double[kol];
	
	
	void (*pokNeparnaFj)(double**, int, int) = NULL;
	pokNeparnaFj = neparniNiz;
	
	pokNeparnaFj(niz, red, kol);
	
	pokNeparnaFj = NULL;
	pokNeparnaFj = ispisNiz;

	pokNeparnaFj(niz, red, kol);
	pokNeparnaFj = NULL;
	*/
	/*
	int red, kol;
	cin >> red >> kol;

	Figura** f = new Figura*[red];

	for (int i = 0; i < red; i++)
		f[i] = new Figura[kol];

	void (*pokFiguraUnos)(Figura**, int, int) = NULL;
	pokFiguraUnos = unos;

	pokFiguraUnos(f, red, kol);

	pokFiguraUnos = NULL;
	
	float* (*pokFiguraProsjek)(Figura**, int, int) = NULL;
	pokFiguraProsjek = prosjekVisine;

	float* prosjekPok = NULL;
	prosjekPok = prosjekVisine(f, red, kol);

	cout << "Prosjek: " << *prosjekPok << " adresa: " << prosjekPok;
	*/



}